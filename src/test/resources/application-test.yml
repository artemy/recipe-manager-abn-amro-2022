spring:
  datasource:
    url: jdbc:tc:postgresql:15.1-alpine:///recipemanager
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
